// No real identity source yet — 001's RoleAssignment model isn't built, so this is a static
// placeholder, not a fetched Person/JWT claim — see docs/specs/006-post-login-home-shells.md's
// Non-goals.
export const MOCK_PLAYER = { name: 'Alex Player' }
